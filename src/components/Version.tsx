import type { FC } from "react";

const Version: FC = () => {
    return (
        <div
            className="p-2 text-gray-100 text-center"
        >
            <span>v0.0.1</span>
        </div>
    );
}

export default Version;