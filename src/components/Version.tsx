import type { FC } from "react";

const Version: FC = () => {
    return (
        <div
            className="p-3 text-gray-100 text-center bg-gray-700"
        >
            <span>v0.0.1</span>
        </div>
    );
}

export default Version;